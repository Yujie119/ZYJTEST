# 问题二实现与现有结果：fresh 独立审阅

日期：2026-09-24。审阅方式：fresh same-family agent，`review_independence: same-family`，`acceptance_status: provisional`。只修改本审计目录；未安装包、未重跑整个正式管线、未改正式源码或结果。所用技能为 `D:/研究生/.agents/skills/experiment-audit/SKILL.md`；项目下同名路径不存在。输入哈希见 `input_hashes.json`，原始题文与工作簿定位见 `raw_task_paragraphs.txt`、`raw_workbook_cells.txt`。未把历史审计结论、历史目标值或他人结果当成真值。

**Overall verdict: WARN；integrity_status: warn。** 当前提交方案的真实四目标、原始约束和导出表可核对成立；未发现假造货箱真值、虚构本轮原始指标或将累计作业时间冒充完成时间。却已找到固定路线和固定资源下严格支配正式代表的可行时序，说明终端搜索明显不足；另有ALNS退火路径被求解器回退和词典序上界实质封住、正式入口强依赖历史数值、范围指纹不足等实现问题。不能把本结论简化为“代码全部无误”。

## A. Ground Truth Provenance — PASS（附假设边界）

数据加载来自原始XLSX及DEM MAT（`问题二/q2_data.py:76`、`:89`、`:103`、`:112`），不是从最终方案生成需求。医疗与首批取适用硬截止的最小值（`:93`—`:101`）；这与原题问题二第18段一致。31硬箱、49软箱属于原始数据分类。正式核验器从传入的 `data` 重建物理量（`问题二/q2_verify.py:43`—`:99`），但共用 `data['geos']`，所以它本身不是独立地形实现。

本轮额外写了 `independent_raw_verify.py`，**不导入任何q2模块**，直接读三张原始工作簿及原始GeoTIFF；独立采用像元矩形/slab相交，不调用生产 `pixel_cover()`。GeoTIFF是WGS84、PixelIsPoint，作半像元边缘转换后与MAT的transform最大差 `3.8489e-18`，全部像元相等（`independent_raw_verification.json:2`）。独立重算原方案与时序见证各426项均通过（`:8`、`:2884`），包括逐箱交付、硬截止、容量、能耗、SOC、资源区间和四目标。

原题仅明确能量分解而未给水平标定与爬升具体关系；当前 `E_use*d/L(q)` 与 `mgh/eta`（`问题二/q2_data.py:158`—`:163`）应理解为正文假设2—3（`正文/正文.md:17`、`:19`），不能称作题目唯一强制公式。准备开始占用滿电池、同站统一以交接完成送达、不加附件外工位/充电器数均已在正文假设5声明（`:23`）。它们是模型假设，不是本轮发现的编码错误。

## B. Score Normalization — WARN（决策尺度来自预实验）

冻结尺度确实来自 `counter + baseline_answers` 的分量最小/最大值（`问题二/q2_run.py:132`—`:136`），不是原题提供的物理常量或已认证理想点。不同预实验会改变尺度及代表选择。因此不能将归一化/遗憾解释为跨实例固定性能分数。

但当前用途是明确披露的多目标决策标度，不是预测准确率；没有把模型输出当标签以制造99%准确率。原始四目标与尺度同时公开，正文说明尺度冻结、不裁剪且非全局理想点（`正文/正文.md:1038`—`:1047`），故不按“伪造性能分数”判FAIL。式中减去参考值的常数在遗憾差中抵消，代码直接用 `w_j/scale_j` 的未平移目标正确。

逐一重算8个档案方案的遗憾区间，与JSON全部一致（`result_consistency.json:391`）；各极点上界确为同一档案完整方案的最小加权值，系数与冻结尺度一致。现有单目标界 `[20,60.70380815279539,6006.72573493983,0]` 来自对应同K0求解器对偶界，并减去声明容差；没有把任意未求完LP迭代值当下界（`问题二/q2_joint.py:202`—`:209`；`问题二/q2_run.py:127`—`:173`）。12个偏好MILP全限时，12个LP只有4个Optimal；区间不能称精确遗憾。

## C. Result File Existence — PASS（数值及导出确实存在）

正式结果 `(28,75.71131596897939,7567.094096939377,200.565886509374)` 与原始独立复算一致。当前最终文件SHA256为 `f5ada42c6d0787f6bb482ee30201e47b129373aad4ba668438badd1a24539459`，与求解摘要匹配，输入指纹也与当前文件匹配（`result_consistency.json:2`—`:4`）。

独立回读官方工作簿 `Q2_运输架次` 的28行与 `Q2_逐箱交付` 的80行；表头对原始模板，单元对同一方案，均通过（`result_consistency.json:638`）。报告模块也先核验同一方案后才导出（`问题二/report_q2.py:38`—`:59`、`:99`—`:122`）。正文现有数值与来源说法匹配（`正文/正文.md:1224`、`:1271`），10种子、敏感性和正式方法比较留作待执行；未发现把占位实验写成已完成结果。

**限制：** 这只证明数值真实性与当前导出一致性，不能证明正式方案不可改进。下面的严格支配见证与“当前档案内非支配”可同时成立，却排除了该方案为K0内Pareto有效的结论。

## D. Dead Code / Execution Path — WARN（实际退火探索受阻）

四目标 `JointModel.f` 均在单目标、ε或偏好任务实际调用（`问题二/q2_joint.py:125`—`:130`；`问题二/q2_run.py:122`—`:162`），独立核验和导出回读也确实进入流程，未发现挂名的主要指标函数。

**I02：ALNS较差解接受机制存在控制流缺陷。** ALNS始终把当前完整方案作为warm start（`问题二/q2_run.py:78`—`:80`）；首阶段最小化C，`solve()`在新解较差时回退warm（`问题二/q2_joint.py:200`—`:201`）；后续词典序阶段再加 `C <= incumbent C + 1e-5`（`:214`—`:222`）。因此可观的C恶化在到达 `q2_run.py:83`—`:87` 的退火接受分支之前已被消除。温度公式存在，不等于具备接受较差可行解的探索通道；可达的至多是容差/浮点波动，不是有意义的退火步。实际15次日志只接受3次：C下降约93.981619、40.285869秒及C不变的后续指标改善，没有较差主目标（`result_consistency.json:531`）。正文 `:1105` 的“不一定出现”应进一步说明结构上被限制。

这是**算法实现与预期机制不一致**，不是题目硬约束错误。应保留当前通道作为强化修复，另设能返回合法较差候选的探索修复并进行受控可达性测试。不能通过增加迭代次数修复该机制。

**I03：正式主入口被历史回归数值耦合。** `问题二/q2_run.py:103` 必须加载 `初始化/审计基线方案.json`，`:107` 要求10秒限时修复达到固定 `520.7998072166129`，否则整个正式流程异常退出。该历史值不是原始真值；换机器/求解时间导致未达到目标并不等于物理错误。`问题二/audit_q2_implementation.py:70` 附近及`:99` 附近另有历史数值回归，作为单独回归可以，但不能当全模型正确性证明。正式入口应将历史暖启动设为可选，把固定数字断言移到历史测试。

## E. Scope Assessment — WARN（有严格改进见证）

**I01：正式代表未达到固定路线、固定资源范围内非支配。** 本轮没有扩库，没有改物理参数，没有使用他人结果；只固定正式28条候选和每条具体无人机/电池，在原四目标上限下最小化L。得到：

| 方案 | N | E / kWh | C / s | L / s |
|---|---:|---:|---:|---:|
| 正式代表 | 28 | 75.71131596897939 | 7567.094096939377 | 200.565886509374 |
| 固定路线与资源的时序见证 | 28 | 75.71131596897939 | 7567.094096939377 | 70.64808661848737 |

L减少 `129.91779989088663` 秒（约64.78%），其他三目标完全相同。11条任务开始时刻变化，路线/资源身份保持（`witness_timing_changes.json:1`）。见证求解状态Optimal，残差 `3.64e-12`，生产求解器内部耗时约0.0073秒（不含Python启动/建模；`fixed_resources_timing_witness.json:3797`—`:3823`）；原始工作簿+GeoTIFF另行426项复核通过。Optimal只认证这个固定路线/资源、带原目标上限的L子问题，不认证全部问题全局最优。

正式代码曾全库进行终端支配检查（`问题二/q2_run.py:179`—`:191`），但该任务实际Time limit reached、0分支节点、约150.56秒并保留原方案（`子问题二/完整联合运行日志.json`末任务；`result_consistency.json:326`）。限时未找到支配解不能证明不存在；这里见证说明廉价的固定结构压紧缺失。**归类为搜索不充分，而非C定义或物理约束错误。** 统一增加分级精修比继续盲目扩大全库MILP更直接。若后续将见证加入档案，必须统一更新全部极点可行上界、遗憾区间和最终选择，不能只替换某一行L。

**I04：候选库大幅受限。** 完整单点目录19525条，正式K0只保留477条单点，遗漏19048条；全库558条，含81条多点。`问题二/q2_candidates.py:29`—`:34` 的局部评分排名并非全局双资源调度的无损支配规则。`:51` 与`:61` 用不放回抽样，所以当前多点生成没有重复访问，实际K0重复访问候选数为0；虽然 `make_route` 支持非相邻重复访问，支持数据结构不等于执行了相应搜索。随机路线限2—5站、医疗为主；`:41`—`:45` 的S001水箱配对及`:56`的指定四站路由属于人工补种。它们不能证明完备性，也不能单独认定不合法。

当前文字已披露有限K0与未证明全局最优，故不作虚构完整范围判FAIL。已认证 `N=20`、`N<=19`不可行都只适用于当前K0。全流程一个场景、一个预实验主种子；40个初始化和15个局部迭代不是40/15次独立正式实验。直接阶段无新增初始化目标向量，ALNS贡献了另外3个现存档案向量。不要从其中推断同预算方法优越性。

## F. Evaluation Type — PASS / simulation_only

本题是附件驱动的确定性运输调度与物理核算，使用真实附件作为参数，不具备飞行实测或“最优调度真值”标签；应分类 `simulation_only`。独立原始量复算是模型内的确定性证据，不能升级成真实风场、电池或通信环境实测保证。时序见证是经独立规则核验的反例，不是用模型输出伪造参考真值。

## 重点：Cmax、下界与安全截断

原题P19明确全部任务完成时间为所有运输无人机完成最后架次并返回O01的最晚时刻（`raw_task_paragraphs.txt:19`）。当前实现符合：

1. `问题二/q2_joint.py:86`—`:87` 建立 `cm >= s_k + duration_k*x_k`。未选候选的s由`:68`强制为0；因此是选中任务返场的上图变量。正C权重时目标会压紧，ε/其他目标时它可能松弛，但不应直接当实际完成时间。
2. `q2_joint.py:166`—`:181` 按已选方案顺序压紧两资源DAG；没有最早服务窗，推早不恶化硬截止/C/L，N/E不变，固定外部任务保留绝对时刻。该压紧不改变同资源的先后次序，因此无法替代本轮见证中的重排。
3. `问题二/q2_data.py:183`—`:197` 从解码后真实 `finish=start+duration` 取max；`:159`—`:168`的事件递推含最终空载返场。`q2_verify.py:90`—`:111`重算相同量；报表用该真实目标与finish导出（`report_q2.py:54`—`:58`）。
4. 实际方案：最后交付7063.134739秒、最后返场7567.094097秒、末电池满充9543.935150秒、所有架次累计duration为49084.073942秒（`result_consistency.json:5`—`:8`）。最后返场为U07的Q2-0028，恰等于U07累计占用7567.094097秒，是该机在原排程无间断占用的结果，不能据此误以为所有duration求和。
5. `q2_joint.py:51`—`:57` 的H是最多n_box条正边资源路径的duration+charge和，加最大固定开始时刻；在当前无释放窗、可推早模型下保留至少同样好的时序。对正权标量目标，`q2_run.py:26`—`:29` 的 `C <= U/alpha_C` 在四目标和权重均非负时是安全目标次水平集截断。当前12极点的C权均正，前提满足；不能随意用该C截断认证另一个目标的全局下界。
6. `global_anchor_C` 用已有可行C上界截断，它保留C最优值，所以对应求解器对偶界可用于当前K0的无条件C下界。偏好标量截断同理保留该标量最优值。限制当前库本身的下界不能认证全部未生成路线。

## 其他工程缺陷与适用边界

**I05：范围指纹只哈希候选ID，不绑定系数。** `q2_data.py:136`—`:178` 的ID仅取机型和逐箱访问；`q2_run.py:115`、`report_q2.py:45`只哈希ID序列。内存中把该路线机型速度乘0.99，ID不变而duration从1626.175046变成1630.288209（`result_consistency.json:652`）。这是实证的范围指纹不足。当前输入指纹与正式结果均匹配，尚无证据本轮混用旧界；风险是下一版修改参数/系数而ID相同后，旧下界/报告可被错认同范围。应将原始数据、假设版本、候选完整系数与约束配置绑定不可变运行ID。

终止状态区分整体基本正确：只接受经矩阵整数/变量界校核的解，保留经过验证的warm；LP非最优不提供最优目标值；词典序限时后续标为条件改善。当前记录39阶段：6 Optimal（含4 LP与一个后续条件N阶段）、32限时、1不可行（`result_consistency.json:52`）。不能把6个Optimal汇总为6个完整多目标认证。求解器30秒参数多次实际113—238秒，日志确实披露；仅靠调用时限不能保证总墙钟预算。

下一版策略提出的原始契约、输入检查、生产矩阵独立小实例、通用候选、强化/探索分离、分级精修和完整范围指纹有必要，但它们在策略中是计划，不是现有实现事实。建议保留共同 `JointModel`，不因现有搜索质量弱就重写题目目标或调整物理参数。

## 建议优先级与可支持结论

1. 优先加入每个待选方案的固定路线/资源精修，再逐级开放资源和局部候选；本轮见证先保留审计目录，正式发布前重评整个共同档案。
2. 分离ALNS强化与探索；把历史回归数字从生产入口移走；以生产矩阵小实例和原始规则为验收依据。
3. 为候选库及下界建立含物理系数的范围指纹，并将每次求解/报告固定到同一不可变运行目录。
4. 扩库与同预算重复实验分别执行；当前数据只能支持“当前假设下完整可行，当前档案内上界代表”，不能支持K0内Pareto有效、精确最小最大遗憾、全部多点路线最优或方法普遍优越。

复现命令（均仅写审计目录）：

```powershell
& 'E:/tool/anaconda3/python.exe' -X utf8 '问题二/源头核查_20260924/implementation/fresh_checks.py'
& 'E:/tool/anaconda3/python.exe' -X utf8 '问题二/源头核查_20260924/implementation/independent_raw_verify.py'
& 'E:/tool/anaconda3/python.exe' -X utf8 '问题二/源头核查_20260924/implementation/result_consistency.py'
```

微型完整枚举真值与生产矩阵的比对由主代理独立执行，不在本报告中冒称本代理已完成。未获得原题提供的官方调度评价器；本轮不声称全部代码路径、异常输入或连续问题最优性均已穷尽证明。

## 审阅后补充搜索（用户另行授权）

完成上述审阅后，用户新增“模型核查无误后尽可能多搜索”。本代理保留正式28条候选，开放全部无人机、电池及时间/顺序，以前述见证暖启动，先最小化L，再在L数值为零下最小化C，取得 `(28,75.71131596897939,7264.62791044095,0)`。两个阶段均Optimal，独立原始数据各426项通过。C比正式方案减少302.466186498427秒，N/E不变，L降至0。这加强了I01的结论，但不改变“现有模型物理可行、搜索不充分”的归类。

材料单独保存在 `问题二/改进搜索_20260924/固定箱组全资源/`，见 `README.md`、`best_plan.json`、`C_solver.json`、`C_verification.json`及 `search_summary.json`。认证仅为固定28候选下先L后C的词典序子问题；仍未认证完整候选问题。正式结果未替换。
