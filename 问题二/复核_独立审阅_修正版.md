# 问题二独立审阅（冻结修正版）

审阅时间：2026-09-24（冻结产物复核）。本报告是上一轮 [复核_独立审阅.md](复核_独立审阅.md) 的修正版历史续审；未覆盖上一轮报告。

**总体结论：WARN（修正后未发现新的实际物理/核验实现错误；仍存在候选范围、限时和未执行重复实验的证据边界）。**

本轮只读读取了原题、正文第5章、冻结源码、配置、日志、结果JSON、提交Markdown/XLSX及复现指纹。复现指纹的 inputs/outputs 全部存在且SHA256匹配。未启动新的长时间主求解；运行了35项回归的无写入重读，回读最终方案、8个档案方案并核对遗憾区间公式。

## A–F

### A. 数据与参考来源：PASS

原题问题二要求联合确定箱组、访问顺序、机型、具体无人机、电池和开始时刻，并满足物资时限和资源周转（DOCX段落P18–P20）。q2_data.py负责从原始工作簿和DEM读取箱、机型、实体无人机、电池、节点和航段几何；q2_joint.py:88–101建立逐箱覆盖和交付/迟到变量，:122–129建立资源占用和四目标。没有发现把模型输出伪造为外部真值或把自身最大值归一化成性能分数的行为。

### B. 目标、归一化与偏好：PASS（精确遗憾仍未认证）

q2_joint.py:126–129的四目标是架次数、能耗、最晚返场和普通箱加权迟到；q2_run.py:127–136冻结参考尺度，:160–173为同一K0及同一尺度的12个L1偏好极点建立MILP/LP上下界，:174–191按上下界计算各档案遗憾区间。偏好权重实际为非负、和为1、L1半径0.3；回归中weight_polytope_0.0/0.3/1.5均通过。

单目标有效下界.json显示同一K0下界为N=20、E=60.70380815279539、C=6006.72573493983、L=0。偏好极点基准的每个下界等于单目标下界加权值、有效LP下界和限时MILP下界三者最大值；逐项回算与JSON误差小于5×10^-16，最终方案上界等于档案最小上界。当前仍不能把区间[0.2179084629852588, 0.5275704112880106]称为精确最小最大遗憾，原因是多数偏好MILP/LP限时以及K0不完整。

### C. 结果存在、来源和导出一致性：PASS

求解摘要.json给出的代表向量为(28, 75.71131596897939, 7567.094096939377, 200.565886509374)，来源为initializer_seed_202607_mode_3，直接阶段相对初始化新增目标向量数为0。最终方案JSON保存source、initialization_seed=202607和initialization_mode=3；重新调用construct_initial得到的方案与最终方案（去除遗憾字段）完全一致。联合非支配档案8个方案均能独立重算、无严格支配对。

report_q2.py:39–53重新核验最终方案和全部档案、核对候选库哈希与归属；:55–60按原模板写入Q2页；:118–126重新打开XLSX并逐单元比较。求解摘要标记template_readback_passed=true，复现指纹当前无缺失或哈希不匹配。问题二求解结果.md:3–5、:23–52使用同一28架次方案，官方提交页没有混入旧20架次方案。

### D. 死代码/实际调用：PASS（相对上一轮已修正）

上一轮指出local_expansion存在但未调用。冻结q2_candidates.py已移除local_expansion；q2_run.py:15只导入single_point_catalog、global_pool、construct_initial，:62–98的ALNS使用固定K0邻域，:156–158明确作为独立简化对照。当前没有把未调用函数当作已执行主流程的证据。

### E. 范围、可复现性和实际求解状态：WARN

上一轮的无序set随机缺陷已关闭：q2_candidates.py:78–85对remaining排序后再取随机数；q2_run.py:42–60按配置顺序合并并行任务结果；q2_run.py:117–120保存初始化方案。实现审计回归.json的initializer_independent_of_hashseed为true，35/35项全部通过，包括consistent_double_booking_rejected和consistent_hard_deadline_violation_rejected。

仍有三项范围边界：

- K0仅558条候选，来自19525条完整单点目录和有限多点抽样；联合运行摘要明确“not the full original feasible route set”。正文第5章:1224、:1271和问题二求解结果.md:5、:89–95均已披露。
- 只有K0内架次数N=20达到整数最优；E、C、L及12个偏好阶段多数限时。正文:1224以及问题二求解结果.md:87–100记录了上下界和状态。最终代表的全局最优、完整Pareto前沿和库内精确稳健最优仍未认证。
- 10种子同预算对照、正式消融、候选扩库和资源/偏好敏感性仍是计划。正文:1477及问题二求解结果.md:95明确没有把单次运行冒充重复实验。

### F. 评价类型：simulation_only

仍属于原题标准场景下的物理/调度仿真与程序核验，不是实际飞行测试，也没有外部最优标签。双GPU复算只复核候选物理量，不是MILP加速或最优性证明；q2_gpu_audit.py:1–5、:76–79明确了该范围。双GPU产物显示19606条候选、两卡均通过，误差约10^-16至10^-13，支持物理重算一致性。

## 数学模型和核验器复查

- **覆盖、硬时限、迟到：PASS。** q2_joint.py:88–101精确覆盖；q2_verify.py:63–81从原始箱属性和事件重建交付、latest_start与缓存；实现审计回归的candidate identity、row candidate、latest_start、双重占用和硬时限违约注入均拒绝。
- **双资源排序与大M：PASS（当前模型范围）。** q2_joint.py:102–124使用共享排序位；同一候选对如同时共享无人机和电池，正持续时间保证顺序一致；:119–121使用各任务有效开始上界加任务/充电占用构造停用常数。q2_joint.py:52–58的H按最多80个路线时长加充电构造，并在固定任务/C上限时收紧。该论证只适用于冻结候选库与声明的压紧调度模型。
- **限时可行解：PASS。** q2_joint.py:198–211区分optimal与incumbent，检查残差/整数性后保留限时可行解；回归limit_incumbent_kept_and_checked通过。
- **ALNS主从关系：PASS/WARN。** 正文第5章:1034、:1083–1087已改写为直接联合MILP主方法、ALNS固定K0简化对照；q2_run.py:62–98实际执行15次ALNS迭代。正文同时明确本轮不生成库外路线、不动态扩大释放窗口。两方法预算不同，不能作等预算优劣或速度结论。
- **导出统一性：PASS。** report_q2.py:39–53以最终方案和同一档案重算；:99–126重写并回读官方工作簿、生成资源事件和比较表；最终Markdown及摘要中的28架次和四目标与JSON一致。

## 仍需保留的优先边界

1. 当前结果是“冻结K0内的经核验近似非支配方案”，不是完整路线空间全局最优。
2. 遗憾区间是共同K0和冻结尺度下的上下界；偏好下界部分来自单目标有效界和4个最优LP，其他LP/整数偏好子问题限时。
3. 物理闭合和作业口径（航程校准、势能爬升、准备开始占用满电池、整批交接送达、不设额外充电器/工位容量）已在正文和审计修正报告中公开，但仍属于建模假设。
4. 10次重复、消融、候选扩库和敏感性实验尚未完成；不得把当前一次冻结运行推广为稳定性结论。
5. 私有HiGHS接口仍是环境依赖，复现指纹和当前环境记录只能保证本冻结环境的复现入口。

**最终判定：WARN。** 先前发现的固定种子、候选身份/latest_start核验、提交导出陈旧和偏好下界过松问题已分别修正或量化；当前未发现新的实际实现错误。WARN仅由有限候选、限时认证和未执行重复实验等证据范围造成。

